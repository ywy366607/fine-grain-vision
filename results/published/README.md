# Published metrics

JSON / text snapshots that back the claims in the root README.

Regenerate with `scripts/train_benchmark.py` and `scripts/line_recon.py`.
Other raw logs under `results/` (if present locally) are gitignored.
